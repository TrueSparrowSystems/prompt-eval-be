apiVersion: v1
kind: Service
metadata:
  name: {{ include "promt-eval.fullname" . }}
  labels:
    {{- include "promt-eval.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "promt-eval.selectorLabels" . | nindent 4 }}
