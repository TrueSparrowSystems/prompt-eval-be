apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "promt-eval.fullname" . }}-test-connection"
  labels:
    {{- include "promt-eval.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "promt-eval.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
